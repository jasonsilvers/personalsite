/** @type {import('twind').Configuration} */
const config = {
  theme: {
    extend: {
      screens: {
        standalone: { raw: '(display-mode:standalone)' }
      }
    }
  }
}

export default config
